import React from 'react';
import Base from '../Base';

const Main = () => (
  <Base
    page={() => (
      <div className="uk-container">
        <h1>Página - Home</h1>
      </div>
    )}
  />
);

export default Main;
